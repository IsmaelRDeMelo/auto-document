[pytest]
testpaths = tests
addopts =
    --cov=.github/skills/data-deploy/scripts
    --cov-report=term-missing
    --cov-fail-under=90

[coverage:report]
exclude_lines =
    pragma: no cover
    if __name__ == .__main__.:
    raise NotImplementedError
