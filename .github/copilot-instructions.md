# Copilot Instructions

When working on a pull request in this repository, check the branch name.

If the branch name matches the pattern `feature/<JIRA-ID>-*` (for example `feature/RMS-1025-add-kpi`)
and the pull request contains changes to files under `migrations/` or `models/`,
use the **data-deploy-doc** skill to generate and publish the deploy documentation.
